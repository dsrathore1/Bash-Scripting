name: CI/CD Workflow for Bash scripting

run-name: 🚀 Test triggered by ${{ github.actor }} on branch ${{ github.ref_name }} # Context variables

on:
  push:
    branches: [Basics]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run test
        run: |
          echo "🟢 Running state"
          # Stopping state
          echo "🚫 Stopping state..."
      - name: 🥗 Run Bash file
        run: |
          ./Arrays.sh
